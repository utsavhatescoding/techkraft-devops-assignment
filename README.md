# IT Infrastructure and DevOps Trainee Assignment

This project demonstrates a secured Ubuntu environment and a containerized
web stack using Nginx, Flask and PostgreSQL.

## Architecture

- Nginx reverse proxy exposed on host port 80
- Flask application running internally on port 5000
- PostgreSQL database with persistent Docker storage
